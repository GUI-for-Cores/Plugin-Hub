const onRun = async () => {
  await Plugins.alert('提示', '内测版已升级为正式版，此插件已弃用，你可自行移除本插件。请安装或更新【滚动发行】插件，以获取最新的 GUI.for.SingBox。')
}

const onStartup = onRun

const onReady = onRun

const Rolling = onRun

const Recovery = onRun

const Changelog = onRun
