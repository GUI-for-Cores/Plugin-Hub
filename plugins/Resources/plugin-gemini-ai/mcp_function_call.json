[
  {
    "name": "user_profile",
    "description": "查看用户软件的偏好设置，包括语言、主题、颜色、字体等等"
  },
  {
    "name": "list_profiles",
    "description": "列出所有的profiles(配置)信息"
  },
  {
    "name": "search_profiles_by_keywords",
    "description": "根据关键词搜索匹配的profiles(配置)信息",
    "parameters": {
      "type": "object",
      "properties": {
        "keywords": {
          "type": "array",
          "items": { "type": "string" }
        }
      },
      "required": ["keywords"]
    }
  },
  {
    "name": "list_subscriptions",
    "description": "列出所有的subscriptions(订阅)信息"
  },
  {
    "name": "search_subscriptions_by_keywords",
    "description": "根据关键词搜索匹配的subscriptions(订阅)信息",
    "parameters": {
      "type": "object",
      "properties": {
        "keywords": {
          "type": "array",
          "items": { "type": "string" }
        }
      },
      "required": ["keywords"]
    }
  },
  {
    "name": "update_all_subscriptions",
    "description": "更新所有的订阅(subscriptions)信息"
  },
  {
    "name": "update_subscriptions_by_keywords",
    "description": "根据关键词搜索匹配并更新相关的订阅(subscriptions)信息",
    "parameters": {
      "type": "object",
      "properties": {
        "keywords": {
          "type": "array",
          "items": { "type": "string" }
        }
      },
      "required": ["keywords"]
    }
  },
  {
    "name": "list_rulesets",
    "description": "列出所有的rulesets(规则集)信息"
  },
  {
    "name": "search_rulesets_by_keywords",
    "description": "根据关键词搜索匹配的rulesets(规则集)信息",
    "parameters": {
      "type": "object",
      "properties": {
        "keywords": {
          "type": "array",
          "items": { "type": "string" }
        }
      },
      "required": ["keywords"]
    }
  },
  {
    "name": "search_rulesets_by_keywords_in_ruleset_hub",
    "description": "根据关键词在规则集中心(ruleset-hub)中搜索匹配的rulesets(规则集)信息",
    "parameters": {
      "type": "object",
      "properties": {
        "keywords": {
          "type": "array",
          "items": { "type": "string" }
        }
      },
      "required": ["keywords"]
    }
  },
  {
    "name": "update_all_rulesets",
    "description": "更新所有的规则集(rulesets)信息"
  },
  {
    "name": "update_rulesets_by_keywords",
    "description": "根据关键词搜索匹配并更新相关的规则集(rulesets)信息",
    "parameters": {
      "type": "object",
      "properties": {
        "keywords": {
          "type": "array",
          "items": { "type": "string" }
        }
      },
      "required": ["keywords"]
    }
  },
  {
    "name": "list_plugins",
    "description": "列出所有的插件(plugins)信息"
  },
  {
    "name": "search_plugins_by_keywords",
    "description": "根据关键词搜索匹配的plugins(插件)信息",
    "parameters": {
      "type": "object",
      "properties": {
        "keywords": {
          "type": "array",
          "items": { "type": "string" }
        }
      },
      "required": ["keywords"]
    }
  },
  {
    "name": "list_plugin_hub",
    "description": "列出插件中心(plugin-hub)中所有的插件(plugins)信息"
  },
  {
    "name": "search_plugins_by_keywords_in_plugin_hub",
    "description": "根据关键词在插件中心(plugin-hub)中搜索匹配的plugins(插件)信息",
    "parameters": {
      "type": "object",
      "properties": {
        "keywords": {
          "type": "array",
          "items": { "type": "string" }
        }
      },
      "required": ["keywords"]
    }
  },

  {
    "name": "list_scheduled_tasks",
    "description": "列出所有的计划任务(scheduled_tasks)信息"
  },
  {
    "name": "search_scheduled_tasks_by_keywords",
    "description": "根据关键词搜索匹配的scheduled_tasks(计划任务)信息",
    "parameters": {
      "type": "object",
      "properties": {
        "keywords": {
          "type": "array",
          "items": { "type": "string" }
        }
      },
      "required": ["keywords"]
    }
  },
  {
    "name": "validate_cron_expression",
    "description": "验证提供的 cron 表达式是否有效",
    "parameters": {
      "type": "object",
      "properties": {
        "expression": {
          "type": "string"
        }
      },
      "required": ["expression"]
    }
  },
  {
    "name": "exitApp",
    "description": "退出程序"
  }
]
